import { CONFIG } from 'src/config-global';

export const ASSETS_PATH = `${CONFIG.site.basePath}/assets`;
export const IMAGES_PATH = `${ASSETS_PATH}/images`;
export const COVER_PATH = `${IMAGES_PATH}/cover`;
